# AuthModules Core

AuthModules Core is the headless identity orchestration core for TypeScript apps.

This package is the first minimal baseline. It currently contains only package scaffolding.

Runtime adapters, storage adapters, auth methods, and UI are intentionally out of scope.
